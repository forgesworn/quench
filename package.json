{
  "name": "@forgesworn/quench",
  "version": "0.0.0",
  "private": true,
  "description": "Decide when an agent has gathered enough evidence to stop",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "quench-labels": "src/cli-labels.mjs"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\"",
    "check": "npm test"
  }
}
